%h1 Listing users

= select "role_name_filter", "role", @roles, {:include_blank => false, :prompt=>'<i>*Filter by role*</i>'.html_safe} , :class=>'toolbarChoice'

= render :partial => 'users_list'

%br

= link_to 'New User', new_user_path
